__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/
.uv-cache/
.playwright-browsers/
build/
dist/
*.egg-info/
.coverage
htmlcov/

# Backup files from BMad install / installer reruns.
*.bak

# pngquant binary built locally by scripts/install_pngquant.sh
tools/bin/

# Claude Code project-local install (skills installed by `bmad install`).
# Re-create on a fresh clone with `bmad install` reading _bmad/config.toml.
.claude/

# Tailwind standalone CLI binary cache (PRD-v0.6.2 — `make tailwind-build`).
.tailwind/

# pytest-benchmark output (PRD-v0.6.4).
.benchmarks/
benchmark.json

# Bench fixture for export-html (regenerated on demand via `make bench-fixture`).
tests/fixtures/bench_100k.sqlite
